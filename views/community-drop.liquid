{% render 'partials/head.liquid' %}

<main>
    <h2>Nieuwste berichten</h2>
    
    <form action="">
        <label for=""></label>
    </form>

    {% for message in messages %}
        <article class="message">
            <h3>{{ message.from }}</h3>
            <p>{{ message.text }}</p>
        </article> 
    {% endfor %}
</main>
